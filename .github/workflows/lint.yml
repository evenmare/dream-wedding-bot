name: Lint

on:
  workflow_call:
  push:

permissions:
  contents: read

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
    - uses: ./.github/actions/prepare
    - name: Lint
      run: ruff check src --no-fix
